name: command_line_solitaire
description: A command-line solitaire game.
version: 1.0.0
repository: https://github.com/ntatko/solitaire-cmd

executables:
  solitaire:
    main: bin/command_line_solitaire.dart

environment:
  sdk: ^3.2.3

dependencies:

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
